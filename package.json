{
  "name": "kerpta",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@kerpta/web",
    "dev:api": "npm run dev --workspace=@kerpta/api"
  }
}